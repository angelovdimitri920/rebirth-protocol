# REBIRTH PROTOCOL — Design Document v1

*A 3D arena mecha roguelite inspired by Custom Robo: Battle Revolution (GameCube), with weapon-loadout depth from Armored Core / Gundam and a real-time roguelike run structure. "REBIRTH PROTOCOL" is a working title, named for the knockdown → rebirth comeback rhythm (§2.2) and the roguelite die-and-retry loop.*

---

## 1. Concept & Pillars

Build a robo from modular parts before a run, fight through a sequence of real-time arena duels, and get stronger *during* the run by picking up boons and items mid-fight — Custom Robo's beloved build-crafting, restructured as a roguelite.

**Pillars:**
1. **Build expression** — every part/boon changes *how you play*, not just your numbers.
2. **Readable, skill-based combat** — no button-mashing; openings are earned, not given.
3. **One more run** — clear direction, real (not fake) choices, escalating power that stays legible.

**Competitive positioning:** No shipped game currently combines (a) a modular pre-battle parts loadout, (b) real-time 3D arena duels, and (c) roguelike run-based progression. Custom Robo's own spiritual successor, *Synaptic Drive* (2020, by series creator Koji Kenjo), has (a) and (b) but explicitly shipped without a roguelite layer — just an arcade score-attack mode. Every existing "mech roguelite" (Wolfstride, Mechabellum, MECHBORN, Mech Armada) is turn-based, an auto-battler, or a deckbuilder. This is open ground.

---

## 2. Core Loop — Inherited from Custom Robo

### 2.1 The five-slot loadout
Before a run (and adjusted between fights), assemble a robo from:
- **Body** — base HP, DEF multiplier, ATK multiplier, air-dash class/identity, charge-attack animation (i-frames during it).
- **Gun** — primary ranged weapon (ATK, bullet speed, homing strength, rate of fire, knockdown power).
- **Bomb** — secondary, slower/AoE, on a cooldown.
- **Pod** — deployable drone/mine for zoning; in our version, pods/funnels get their **own energy pool**, independent of guns/bombs, so they're an always-on pressure tool rather than a burst-damage opportunity cost (Gundam Breaker 4's mistake was tying funnels to the same meter as big attacks, which made them feel unusable — avoid that).
- **Legs** — ground speed, jump height, air-dash count/type, turning, landing recovery.

Any part fits any body. Each body should be a genuine *archetype* (not just a stat-stick) defined by its air-dash behavior — e.g., a balanced 2-dash all-rounder, a long-single-dash glass-cannon flier, a stealth/vanish-dash evader, a slow tank with high load capacity.

Visual/silhouette design basis for these archetypes: see [`ROBOT_SHELL_DESIGN.md`](ROBOT_SHELL_DESIGN.md). Content-expansion map for growing every part slot well past the current 2-4-per-slot roster, plus the damage-balancing philosophy behind it: see [`PARTS_AND_DAMAGE_REFERENCE.md`](PARTS_AND_DAMAGE_REFERENCE.md).

### 2.2 HP, endurance, knockdown, rebirth
Twin-bar system: a large HP pool plus a separate **endurance** bar. Taking hits drains endurance; when it empties, the robo is knocked down (briefly helpless, mash to recover faster), then enters a few seconds of **rebirth invincibility** on standing. This is the source of Custom Robo's signature comeback rhythm — pressure to force a knockdown, but respect the rebirth window. Endurance regenerates over time when unhit.

### 2.3 Arenas ("Holosseums")
Small, wall-bounded 3D arenas with cover geometry (walls, platforms), multi-tier verticality, and environmental hazards (lava, ice, conveyors). Cover matters — you duck behind walls to break homing lock, and arc-firing weapons exist specifically to dig enemies out of cover.

Arena design basis and Stage 1 recommendation: see [`HOLOSSEUM_REFERENCE.md`](HOLOSSEUM_REFERENCE.md).

---

## 3. Differentiators — What Makes This Not Just Custom Robo

### 3.1 Melee weapons (new to the formula)
Custom Robo has no melee combat — this is the biggest single differentiator. Design menu, drawn from Armored Core 6 and Devil May Cry:
- **Separate melee button** from gun/bomb/pod (Devil May Cry's foundational lesson — melee and ranged on different inputs let players weave both rather than choosing one).
- **Gap-closer dash-attack** (Stinger/AC6-kick style): high commitment, punishable recovery if it whiffs, but closes distance on kiting ranged opponents.
- **Combo strings**: 2–3 hit branching chains (stab → sweep → launcher), similar to Gundam Vs beam saber strings.
- **Stagger/poise meter** (AC6's ACS system): melee and high-impact hits build a separate "stagger" gauge; filling it stuns the target for a big punish window. This gives melee a clear job (staggering) distinct from ranged's job (sustained damage), so both archetypes matter.
- **Melee clash**: when two melee attacks connect simultaneously, resolve it via a fast step-cancel (a la Gundam Vs's "Rainbow Step") rather than a rock-paper-scissors minigame — whoever re-engages faster wins the exchange.

### 3.2 Shields (new layer on top of endurance)
Keep HP/endurance as the core Custom Robo system, and add shields as a genuinely distinct layer — don't let it double up with rebirth invincibility (a broken shield should feed into the *same* stagger/knockdown state, not stack a second free defense on top). Two shield archetypes to consider (pick one for v1, both are viable stretch goals):
- **Regenerating energy shield** (Overwatch model): its own bar, absorbs damage, stops regenerating when recently hit, only blocks from the front, regenerates after ~2–3s without damage.
- **Consumable/physical shield** (Gundam beam-shield model): finite, depletes permanently once broken this fight, doesn't regenerate mid-fight (refills between arenas as a run resource), guard-breaks into a stagger when it fails.
- **Shield bash**: turn a successful block into an offensive stagger option (Brigitte/Gundam Breaker precedent) so shields aren't purely passive.

### 3.3 Expanded air combat & flanking
Layer these on top of Custom Robo's existing jump/air-dash:
- **Boost economy**: movement (jump, air-dash, hover) spends a shared gauge that only refills on landing; landing recovery scales with how much you spent, and fully draining it ("overheating") adds an extra penalty. This single rule (borrowed from Gundam Vs) creates most of the aerial skill ceiling — positioning, baiting an opponent's landing, and managing your own gauge.
- **Homing dash** (Zone of the Enders model): a dash that curves toward the locked target — becomes a melee lunge up close, or a homing ranged option at range.
- **Momentum-building movement** (Titanfall 2 principle): if you add wall-contact or slide mechanics, have them *build* speed rather than just repositioning, so chaining movement feels rewarding.
- **Range-gated lock-on** ("red lock / green lock" from Gundam Vs): homing tracking only works within an effective range band; outside it, shots fly straight. Gives ranged weapons meaningful falloff and rewards spacing.
- **Landing-recovery flanking**: reward players who punish an opponent's post-landing vulnerability window — this is the core skill expression of Gundam Vs's aerial neutral game.

### 3.4 Arena variety
Go beyond Custom Robo's relatively flat Holosseums:
- **Destructible cover** and throwable hazards (Power Stone).
- **Dynamic mid-fight events**: randomly-triggered hazards that reshape the arena or force engagements (Anarchy Reigns' tsunamis/black holes/sawblades model).
- **Roguelite modifier rolls**: treat each arena as a roll of {layout, hazard set, environmental modifier}, the same way Hades rolls encounter modifiers and Dead Cells varies biome hazards — keeps repeat runs feeling fresh without needing hundreds of hand-built arenas.

---

## 4. Roguelike Structure

- **Pre-run base kit**: the five-slot loadout is your Hades-style "chosen weapon" — a deliberate, skill-expressing starting identity, not randomized.
- **In-run boons**: offered as **3 choices** after cleared fights, one per ability slot (gun/bomb/pod/melee/dash) — enforce Hades's "one boon per slot" discipline to bound complexity. Boons should change *behavior* ("your dash now leaves a damaging afterimage"), not just add flat stats.
- **Stacking items**: smaller passive pickups that stack linearly for additive effects; use **hyperbolic scaling** (`1 − 1/(1+a·x)`) for any %-chance effect so it approaches but never trivially hits 100% (Risk of Rain 2's model). Hook all items into a small set of universal trigger verbs (on-hit, on-kill, on-knockdown, on-dash) so synergies emerge combinatorially rather than needing hand-authored pairs.
- **Real choices, not fake ones**: every reward option should open new possibilities rather than being an obvious best-pick or an obvious trap. Offer mitigation (rerolls, banish, shop) so a rough early run stays recoverable.
- **Meta-progression unlocks options, not raw power**: new parts/boons/archetypes to draft from, not flat stat increases — keeps runs varied at hour 50, not just hour 5.
- **Fix Custom Robo's own known weakness**: player/critic consensus on the GameCube game is that bombs and pods were often skippable next to a good gun. Make sure early boon/item design gives bombs and pods (and now melee, shields, and funnels) their own clear win conditions so no single tool dominates every build.

---

## 5. Engine & Tech Stack

**Decision: Three.js + TypeScript, bundled with Vite.**

This supersedes an earlier Godot recommendation in this project's research. Worth recording honestly: this trades away Godot's more mature AI-agent tooling (dedicated MCP servers with in-editor run/debug feedback loops) and its built-in editor, physics, and character controller — all of that now gets assembled from libraries instead of coming for free. The upside: Three.js is *just* JavaScript/TypeScript end-to-end, so an AI coding agent can read, write, run, and test it with a normal dev server and no engine-specific tooling required at all — and it opens genuine browser-native distribution (itch.io, web demos) alongside Steam.

**Core stack:**
- **Three.js** (r16x+) for rendering — scene graph, camera, lighting, `GLTFLoader` for mecha models.
- **Rapier3D** (`@dimforge/rapier3d-compat`, Rust compiled to WASM) for physics and the character controller. It ships a built-in `KinematicCharacterController`, which is the right primitive for the boost-economy movement in §3.3 — you want precise control over grounded/airborne state and velocity, not a generic rigid-body simulation. It's a more actively maintained, better-performing choice than Cannon-es for a project with real movement/collision demands.
- **TypeScript**, not plain JS — worth the small setup cost at this scope; it catches a whole class of bugs an agent can otherwise introduce silently across files.
- **Vite** for the dev server and bundler — fast hot-reload matters a lot when tuning movement feel.
- **Howler.js** or native Web Audio for sound; an HTML/CSS overlay (not in-canvas rendering) for HUD/menus — far faster to iterate on than drawing UI in WebGL.

**Distribution:** Three.js runs natively as a browser game, so itch.io (HTML5) is a zero-friction release target alongside Steam. For Steam specifically, wrap the build in **Tauri** (Rust-based, much smaller binary than Electron, the current recommendation for new projects) or Electron if you hit a Tauri/WebGL compatibility snag. Steamworks integration then goes through the wrapper's native layer, not the browser.

**Verification loop:** Since this is a normal web app, an AI coding agent can run `npm run dev` and check the dev console for errors directly; if you have a browser-automation MCP connector wired into your Claude Code setup, that gives it a visual self-check loop roughly analogous to Godot's in-editor screenshot tooling, though it isn't wired up by default the way the Godot MCP servers are.

---

## 6. Staged Development Roadmap

**Stage 1 — Core duel prototype (weeks 1–6).** One arena, one robo, the boost economy (jump/air-dash/hover spending a gauge that only refills on landing), lock-on, one gun + one melee weapon with a gap-closer, and the HP/endurance/knockdown/rebirth loop. *Go/no-go: does the movement-and-punish loop feel tense and good with zero upgrades, against a dummy or basic AI, for 10+ minutes?* If not, stop and fix this before adding anything else.

**Stage 2 — Full loadout + one shield (weeks 6–12).** All five part slots as swappable resources with real stat tradeoffs; one shield archetype wired into the existing stagger/knockdown state. *Go/no-go: do two different builds (e.g., melee-rush vs. funnel-kiter) feel meaningfully different and counter each other?*

**Stage 3 — Roguelite loop + arena modifiers (weeks 12–20).** Run structure across a sequence of duels; boon/item drafting between fights; arena modifier rolls. *Go/no-go: do playtesters describe their build, not just whether they won?*

**Stage 4 — Aerial identity + funnels + melee clash (weeks 20+).** Remote drones with their own energy pool, homing dash, melee-clash/parry counterplay, momentum movement if maps support it.

**Thresholds that should change the plan:** if projectile/particle counts approach bullet-hell scale, look at instanced rendering and Three.js's WebGPU renderer path before assuming a rewrite is needed; if online PvP becomes a core pillar rather than a later add-on, plan a WebSocket/WebRTC layer early — browser-native networking is one area where Three.js is actually at an advantage over a native engine.

---

## 7. Open Design Questions (resolve during prototyping, not up front)

- Exact stagger-meter numbers (fill rate per weapon type, decay rate, stun duration).
- Whether shields are a Stage 2 feature or should wait until Stage 3 once the core loop is proven.
- Whether funnels/pods deploy as persistent companions or single-use consumables.
- How many arena layouts are needed at ship vs. how much variety comes from modifiers alone.
- Whether multiplayer (local or online) is ever in scope, and if so, when to start planning netcode.

---

## 8. Implementation Log — Stage 1 Judgment Calls

Decisions made during Stage 1 prototyping where the design doc was ambiguous or silent. Revisit any of these if playtesting disagrees.

- **Downed robos are fully invulnerable**, not damage-reduced (source games reduce damage). Keeps the comeback rhythm clean in a 1v1; revisit if juggling/oki play ever matters.
- **Melee is a single swing** — no 2–3 hit combo strings yet. §3.1's combo strings are treated as a later addition on top of the proven gap-closer core, not a Stage 1 requirement.
- **No separate stagger meter yet.** Endurance fills that role for now; the dedicated stagger gauge (§3.1) arrives with shields in Stage 2, since a broken shield needs it to feed into.
- **Dash from the ground is allowed** (converts to a small hop + air-dash, costs boost). The design only specifies air-dash; a grounded robo with zero dash options felt wrong.
- **Lock-on is default-on and arena-wide**; the red-lock/green-lock range gate (§3.3) is deferred to Stage 4 as planned, but gun homing already stops beyond 24 m as a soft preview of it.
- **Controls (KB+M):** WASD move (camera-relative), Space jump/hover + mash-to-recover, Shift air-dash, LMB gun, RMB melee, Tab lock-on toggle, R restart.
- **Boost numbers** (gauge 100): hover drain 45/s, air-dash 28, landing recovery 0.1 s + 0.55 s × fraction spent, overheat +0.5 s. All in `src/core/tuning.ts`.
- **Working title: REBIRTH PROTOCOL** (named for §2.2's rebirth window + the roguelite loop).

### Stage 2 judgment calls

- **Shields resolved as a Stage 2 feature** (§7 question closed): the regenerating front-arc energy shield ("Aegis Barrier") shipped first. Guard break feeds *directly* into the existing knockdown state — no separate stagger meter yet, endurance still fills that role.
- **Shield is a sixth slot** (None / Aegis), not folded into body parts — keeps body archetypes about movement identity.
- **Pods are persistent deployables** (§7 question closed): toggle deploy/recall at your position; the drone fires homing chip shots on its own regenerating energy pool, per §2.1's independent-pool rule.
- **Bombs lead their target** (aim at predicted landing-time position) — without this a strafing opponent made bombs literally unhittable, the exact §4 "skippable" trap.
- **Bomb AoE is friendly-fire**: your own bomb can guard-break or knock *you* down.
- **Vanish dash (Wraith) grants projectile i-frames only** — melee still connects, so the evader body doesn't hard-counter melee builds.
- **No charge attacks yet**: body identity = dash type + stat profile for now; charge attacks (§2.1) deferred.
- **Enemy preset build**: Bulwark / Ram Cannon / Quake Bomb / Sentry Pod / Strider / Aegis — a tanky bruiser that contrasts with most player builds for the Stage 2 go/no-go ("do two different builds feel meaningfully different?").
- **Known polish gap**: no camera collision — cover walls can briefly occlude the third-person camera.

### Stage 3 judgment calls

- **Run shape**: 5 duels per run against escalating enemy presets (all-rounder → harasser → shielded skirmisher → tank → kitchen-sink), each with a flat ×(1 + 0.12·fight) power multiplier on HP and ATK.
- **Player HP persists across fights** (+15% max-HP heal between fights); endurance/shield/boost reset per fight. Death ends the run.
- **Boons apply to the player only** — the AI doesn't draft. Drafts offer 3 boons from 3 *different* ability slots (§4's one-boon-per-slot discipline); 1 reroll per run; skipping is allowed.
- **Items drop mid-fight from destroyed crates** (30% chance) as walk-over pickups, rather than from a post-fight shop — makes destructible cover double as an economy. %-chance items use hyperbolic stacking as specified.
- **Arena modifier rolls**: {layout: crossfire/bastion/scatter} × {hazard: none/lava/ice/conveyor}; fight 1 is always hazard-free so run openings stay readable. Hazard damage bypasses shields (environmental, not directional).
- **Mitigation shipped**: reroll only; banish and shops deferred until there's a currency to spend.
- **Meta-progression not started** — §4's "unlock options, not power" layer is still open.

### First playtest feedback (2026-07-13) — controls & camera revision

- **Camera is now the Custom Robo GameCube view**: elevated, fixed-yaw, anchored to the player's side of the arena. It never rotates, so WASD maps 1:1 to screen directions — this also eliminated the "inverted A/D" feel, which was really the old orbiting lock-on camera constantly re-basing what "left" meant.
- **Free facing**: the robo faces its movement direction; it only squares up to the enemy while actually attacking (gun held / melee active). Lock-on (Tab) still governs homing, melee targeting, and homing dash — it no longer steers the camera or facing.
- **No pointer lock / mouse-look anymore** — mouse is buttons only (LMB gun, RMB melee).
- **Arena walls are real**: connected visible perimeter walls with glowing trim enclose the Holosseum; colliders extend invisibly above the visible wall so nothing boosts out. Replaces the old invisible boundaries + floating rim strips.
- Camera-occlusion polish gap from Stage 2 is largely mooted by the high camera angle.

### Second playtest pass (2026-07-13) — control fix + top-down camera + full containment

- **Root-caused the "inverted" controls**: it wasn't the key mapping, it was a sign error in the camera math. The fixed camera looks down world +Z, and in Three.js's right-handed convention that flips which world axis is screen-right (confirmed by the rotation math: a camera facing +Z has local "right" pointing at world −X, not +X). I'd hand-picked the wrong axis twice in a row. Fixed properly this time: movement is now derived every frame straight from `camera.quaternion` (`screenRight`/`screenForward` vectors), so WASD is correct by construction and can't drift out of sync again if the camera is retuned later.
- **Camera pitched much steeper** — confirmed via the wiki's own framing of the series ("frantic action battles in confined 3D arenas, similar to Virtual On") that an elevated, near-overhead read is the right genre reference. Went from ~43° to ~65° (`height: 20, back: 9` in `tuning.ts`), with a full-arena screenshot confirming both robos, cover, and the pickup all read clearly at a glance.
- **Deliberate divergence from the literal GameCube camera**: the original rotates to track the player's facing (Virtual On-style lock-on chase cam). We're keeping a fully fixed-yaw camera instead, because the user explicitly wants free movement without forced facing — a rotating camera would reintroduce the same "what does WASD mean right now" problem this pass just fixed. Worth revisiting if a future pass wants full camera fidelity.
- **Arena is now a sealed volume, not just walled**: added an invisible ceiling collider matching the wall footprint, and raised the invisible wall height from 6 to 34 (verified: a fully-drained max-duration hover peaks at 31.67 before falling back, comfortably under the ceiling). This was a real exploit, not just a request — the boost economy's hover mechanic can carry a robo ~29m up, which cleared the old 6-unit wall collider entirely.

### Stage 4 slice (partial)

- **Homing dash**: while locked on, active dashes curve toward the target at a capped turn rate (ZoE model) — works as approach and as escape-denial.
- **Enemy melee**: the AI now goes for melee up close, preferring to punish the player's landing recovery — landing-recovery flanking (§3.3) now cuts both ways.
- **Melee clash**: simultaneous melee attacks within range cancel both into a 0.25 s step-cancel with mutual knockback — whoever re-engages faster wins the exchange (§3.1's Rainbow Step resolution, no RPS minigame).
- **Still open from Stage 4**: momentum-building wall movement, funnel behaviors beyond the static pod.
- **Verification (2026-07-13)**: all live-verified in the sim — homing-dash curves ~3 rad/s toward the locked target mid-dash; melee clash cancels both attackers into 0.25 s step-cancel locks; lava pools apply continuous HP+endurance DoT; conveyors drift standing robos; ice preserves slide momentum after input release. Only organic AI melee initiation (timer/range branch) hasn't been observed end-to-end — it shares the verified tryStart path.

### Feel pass: lock-on reticle, melee combos, sound (2026-07-13)

- **Red/green lock-on reticle**: closes the Stage 4 gap noted above. A DOM reticle projects onto the enemy's screen position (`Vector3.project`); red inside gun homing range (shots track), green outside it (shots fly straight). Confirmed live: red at 11m (inside the 24m homing range), green at 40m.
- **Melee combo strings** (§3.1): pressing melee again during the recovery of a swing that *connected* chains into hit 2, then a heavier finisher (hit 3) — `[opener ×1, follow-up ×0.85 dmg, finisher ×1.4 dmg/×1.8 knockback]`. Whiffs always end the string in full recovery; only a landed hit opens the chain window.
  - **Bug caught in verification, fixed before commit**: the first version of `chain()` re-swung in place with no re-approach. Knockback from the opener (10 m/s, per `TUNING.melee.knockbackSpeed`) reliably pushed the target just past `hitRange` (3.0) before the follow-up's active frames, so chained hits whiffed almost every time — the feature would have shipped non-functional. Fixed by having `chain()` reuse the same gap-closing lunge `tryStart()` uses when the target drifted out of `closeRange`, extracted into a shared `beginSwingOrLunge()`. Re-verified: opener connects, chain call advances `comboIndex` to 1 and re-enters the swing state, damage accrues across both hits.
  - AI chains reactively (~7 attempts/sec while a swing is in recovery) rather than reading a hit-confirm, so it doesn't always press the string — intentionally imperfect, not every AI swing should become a full combo.
- **Synthesized sound (Web Audio, no audio assets)**: `src/core/sfx.ts` is a small synth module (oscillators + filtered noise, no samples) covering shots, hits, melee swing/hit, shield block/guard-break, clash, explosion, dash, knockdown, rebirth, elimination, pickup, draft pick, and victory/defeat stingers. `AudioContext` only starts on a user gesture (canvas mousedown, hangar DEPLOY click) per browser autoplay policy — `sfx.ensure()` is idempotent and safe to call from every gesture handler rather than threading a single init call through the boot sequence.
- **Verification (2026-07-13)**: the full 3-hit chain now connects end to end (opener → chain re-lunges → follow-up connects → chain again → finisher, 380 total damage across a live sim run) confirming the fix above actually works, not just compiles. All 17 sfx entries were fired through the real `AudioContext` (wrapping `createOscillator`/`createBufferSource` to count nodes: 25 oscillators + 9 noise buffers created, zero exceptions, context state `running`).

### Sound coverage audit + controller support (2026-07-13)

- **Sound audit**: went through every player action looking for silent moments. Found and filled four real gaps: boost overheat (a distinct warning tone, not just silence when the gauge hits zero), landing (a thud on any landing that spent >15% of the gauge — skips tiny hops so it doesn't spam), hangar/draft UI clicks (part selection, reroll, skip all had no feedback), and lock-on toggle (a rising/falling chirp). 21 total sfx entries now. One known limitation, not fixed this pass: no positional audio — enemy and player actions play through the same global bus with no panning/distance falloff, since the arena is small enough that it reads fine without it for now.
- **Xbox controller support** (`Input.poll()` in `src/core/input.ts`, W3C standard Gamepad API mapping): left stick moves (analog direction, no analog-speed model — the sim never had one, digital WASD already always moves at full `runSpeed`), A jumps/hovers/mashes, B dashes, X throws the bomb, Y toggles the pod, LB toggles lock-on, RB melees, RT fires (held), Start rebuilds. Gamepad buttons are merged into the *same* key-code space the keyboard already uses (`"Space"`, `"ShiftLeft"`, etc.) via `Input.held()`/`justPressed()`, so `PlayerController` needed zero changes beyond reading the new `stickMoveDir` for movement — everything else just works through the existing abstraction. Polled once per rendered frame (not per fixed sim step), matching how the event-driven keyboard state already behaves across a frame's multiple fixed steps. `fireHeld` became a getter that ORs mouse and pad trigger state so either input source can hold fire without the other clobbering it.
  - **Scope note**: only in-fight controls got pad support. The hangar and boon-draft screens are still mouse-only DOM UI — full-run gamepad-only play isn't there yet. Worth a follow-up pass if that matters.
  - **Verification**: no physical controller is available in this environment, so `navigator.getGamepads` was mocked with a plain object matching the standard mapping shape (buttons/axes arrays) to exercise the real polling logic. Confirmed: stick deadzone filtering, edge-detection for A/RB/Start (held vs. justPressed-once), RT as a level-triggered hold, and — critically — that stick-right and keyboard D produce the *same direction* of movement (both route through the identical camera-derived `screenRight` vector). Also ran a full 5-fight run to completion afterward with zero console errors, confirming nothing in this pass regressed the core loop.

### Music, full remaining sound gaps, controller legend, fullscreen (2026-07-13)

- **Shared audio core** (`src/core/audio.ts`): one `AudioContext`, one master gain, separate `sfxBus`/`musicBus` sub-mixes so effects and music can be balanced independently. `sfx.ts` now routes through it instead of owning a private context — no behavior change, just lets `music.ts` share the same context cleanly.
- **Six more action sounds**, closing out the audit: bomb throw (previously only the *explosion* had sound, not the throw itself), pod deploy vs. recall (distinct rising/falling chime), crate destruction (wood-crack), thrust/liftoff (a soft whoosh the instant you start holding jump airborne, edge-triggered so it's one cue per liftoff, not a loop), knockdown mash tick (one tick per successful mash press), and a throttled lava sizzle (hazard DoT was applying every physics step — hooking a sound directly to that would've fired 60×/sec, so it's gated to once per 0.4s per robo via a `WeakMap<Robo, number>` cooldown). 27 sfx entries total now.
- **Procedural background music** (`src/core/music.ts`): a lookahead-scheduled step sequencer (the standard Web Audio pattern — poll every 25ms, but schedule notes against `AudioContext.currentTime` so playback timing doesn't jitter with the JS event loop), built entirely from oscillators/noise like the sfx — no audio files. Two loops share one 4-bar A-minor progression (i–VI–III–VII) so switching between them doesn't feel like a genre change: a calm 84 BPM detuned-pad ambient loop for the hangar and boon-draft screens, and a driving 128 BPM four-on-the-floor loop (kick/hats/bass/sparse lead fills) for fights. `music.start()` is a no-op if the requested track is already playing, so it's safe to call from every relevant transition (hangar card click, `Game.startFight()`, `Game.advanceRun()`'s interlude phase) without managing a "did we already start this" flag.
- **Translucent controller legend**: a small glass panel (`background: rgba(10,12,24,0.38)` + `backdrop-filter: blur`) bottom-right, listing every mapped button next to its action (color-coded A/B/X/Y matching real Xbox face-button colors), fading in only while `Input.gamepadConnected` is true so keyboard-only players don't see clutter. Deliberately stays translucent even at full visibility per the brief — it's a reference overlay, not a menu.
- **Fullscreen**: `document.documentElement.requestFullscreen()` fires from the DEPLOY button click (a valid user gesture) and swallows rejection (blocked by browser policy, already fullscreen, etc. — the game is fully playable windowed too, this is a convenience, not a requirement). Added an `F` key as a manual re-entry point in case the browser blocks the automatic request or the player exits and wants back in; `Escape` already exits fullscreen natively, no extra code needed there.
- **Verification note**: the Browser-pane tab was backgrounded for this pass (`document.hidden === true`), which we already knew suspends `requestAnimationFrame`. It turns out the same backgrounding also returns **stale values from `getComputedStyle`** in this environment — confirmed by setting `el.style.opacity = "1"` directly (highest CSS specificity possible) and still reading back `"0"`. So the legend panel's visual fade couldn't be screenshotted or opacity-measured this session. What *is* independently verified: the CSS rules parsed correctly with correct cascade order (`#hud-pad-legend.on { opacity: 1 }` present and correctly beating the base rule), and the `.on` class toggles exactly in sync with `gamepadConnected` going true/false. The music engine's note output was verified precisely instead (see below), which doesn't depend on style computation at all. **Next session with a foregrounded tab: confirm the legend panel actually fades in/out visually.**
- **Music verification**: monkey-patched `AudioContext.createOscillator`/`createBufferSource` to count node creation over real time windows. The hangar loop produced exactly 7 oscillators in a 4-second window — matching the hand-computed prediction from the note pattern exactly (1 bar's pad chord = 3 oscillators, plus 4 bell-arp triggers within that window = 7), which is about as strong a confirmation as a black-box test can give that the scheduler's timing math is correct, not just "produces some sound". The combat loop produced 21 noise hits and 12 oscillators in a 2-second window, consistent with the expected kick/hat/bass density (exact count has some slop from live JS-timer scheduling jitter under test-harness overhead, which is expected and not itself a bug).

## 9. Right/Left Arm Loadout Rework, Unique Part Meshes, Menus, Camera (2026-07-13)

Major playtest-driven pass: the five-slot loadout became six per §2.1's original spec, but with two of the six slots turned into a real *choice* between two categories rather than always-on parts.

### 9.1 Right Arm (gun ↔ melee) and Left Arm (bomb ↔ shield)

`Loadout` no longer has independent `gun`/`bomb`/`shield` fields. It now has `rightArm: {kind:"gun", part} | {kind:"melee", part}` and `leftArm: {kind:"bomb", part} | {kind:"shield", part}` — discriminated unions, so a robo can never be "gun and melee" or "bomb and shield" at once by construction, not by convention. `Gun`/`Bomb` both check their arm's `kind` and silently no-op if it doesn't match (defensive, since nothing should ever call them when the wrong part is equipped, but a silent no-op is safer than a runtime crash if that assumption is ever violated). LMB/RT (gun) and RMB/RB (melee) are *both* always live inputs — whichever isn't equipped just does nothing, so no key rebinding is needed when you swap arms.

**New melee weapon catalog** (previously a single hardcoded weapon in `TUNING.melee`): **Saber** (balanced, matches the old numbers exactly so nothing regressed for existing saves), **Warhammer** (210 dmg/90 endurance, huge knockback, but 0.3s active + 1.4s whiff recovery — the "can't spam it" cooldown the brief asked for, in weapon form), **Twin Fang** (85 dmg, fast, 0.6s whiff recovery — low commitment, low reward). Only the *shared* gap-closer mechanics (lunge range/speed/reach) stay in `TUNING.melee`; everything that makes a weapon feel different (damage, range, arc, timing, knockback) now lives on the `MeleeWeaponPart` itself.

### 9.2 Shields: engage-to-block, not passive-always-on

Reworked from "always intercepts frontal hits automatically" to match the detailed brief: **must be held (Q)** to block at all — releasing it makes the shield inert. Even held, it's never 100%: `frontBlockPercent`/`backBlockPercent` (Aegis: 75%/25%; Bastion: 92%/40%) mean chip damage always reaches HP/endurance regardless of which side you're hit from, just proportionally more from behind. The *blocked* portion drains `shieldHp` (guard-break to knockdown at 0, same as before); the *chip* portion goes through `Health.takeHit()` exactly like an unblocked hit. Holding the shield up costs ground speed (`moveSpeedMult`) and disables air-dashing entirely (thrust/hover still work). **Melee parry**: a melee swing that connects into an engaged shield drains the *attacker's* endurance directly (`Health.drainEndurance()`, a new method that mirrors `takeHit`'s knockdown check but skips HP) — `Melee.checkHit()` checks this after calling `receiveHit()`, since it already has both attacker and defender in scope.

**Verified precisely, not just "runs without crashing"**: direct `receiveHit()` calls confirmed exact front-block (26.25 = 35×0.75), back-block (10 = 40×0.25), and melee-parry (20, exact) numbers; a movement-distance comparison confirmed the speed penalty; a boost-gauge check confirmed air-dash is actually blocked (not just visually implied) while shielded and airborne; a controlled guard-break test confirmed the state transition to knockdown.

### 9.3 Every part has a unique mesh, not a color swap

`RoboMesh.ts` rewritten as a data-driven builder: `buildRoboMesh(loadout, hull, accent)` builds a genuinely different primitive silhouette for each of the 4 chassis (Vanguard baseline / Skylance tall-and-finned / Wraith angular-and-caped / Bulwark wide-with-pauldrons), 3 guns, 3 melee weapons, 2 bombs, 2 shields, 3 leg styles, and 2 pod-mount styles — all consistently attached at chassis-specific right-arm/left-arm/back anchor points so any combination composes correctly. Verified via the hangar's live preview: swapping to a Bulwark/Warhammer/Bastion build produced a visibly wider, differently-proportioned silhouette than the Vanguard/Blaster/Impact default.

### 9.4 Hangar rewrite: live 3D preview + full menu navigation

The hangar is now a split-screen: a rotating 3D preview (its own small `HangarPreview` renderer, using the *exact same* `buildRoboMesh` the real game uses) on the left that rebuilds every time a part is picked, part cards grouped by slot on the right with two-way tabs for Right Arm (Guns/Melee) and Left Arm (Bombs/Shields). Every card, tab, and the DEPLOY button is keyboard *and* gamepad navigable — a `[data-row]`-based focus grid (Up/Down move between rows, Left/Right move within a row, Enter/A activates), driven by both real keyboard events and a dedicated gamepad poll loop (the hangar exists before `Game`/`Input` do, so it can't reuse those — D-pad and stick both drive the same grid, with a repeat-delay so a held direction doesn't scroll every frame).

**Bug caught during verification**: the preview canvas measured 0 height on first layout (`canvas.clientHeight === 0`) because canvas is an inline replaced element and a `flex:1` canvas without `display:block` doesn't reliably stretch in a column flex container across all cases. Fixed with `display:block` plus a `ResizeObserver` on the canvas itself — deliberately *not* a `requestAnimationFrame` retry, since rAF is exactly the kind of thing that can stall (a background tab suspends it entirely; a slow first paint could too), while `ResizeObserver` fires the instant the element's real box actually changes, independent of the render loop.

### 9.5 Pause menu, instant hangar return, isometric camera, endurance

- **Pause** (P key / gamepad Start, remapped from the old instant-rebuild): freezes the fixed-step sim loop entirely (verified: HP doesn't change across 30 forced sim steps while paused) and suspends the shared `AudioContext` so music/sfx scheduling pauses cleanly too. Menu offers Resume, Restart Fight (fresh full-HP retry of the current fight, not wherever your HP happened to be), and Back to Hangar.
- **R still instantly reloads to the hangar** on keyboard, unchanged. Gamepad users get the same result through the pause menu's Back to Hangar option, since Start is now pause instead.
- **Camera switched from perspective to orthographic** for a true isometric read (no perspective convergence) and pushed steeper (`height`/`back` from 20/9 to 24/8, ~72° from horizontal vs. ~66°). `PlayerController`/`Hud`/`Duel` all widened their camera parameter types from `THREE.PerspectiveCamera` to the common `THREE.Camera` base, since they only ever touch `position`/`quaternion`/`lookAt`/`project` — none of that needed to change.
- **`maxEndurance` doubled (100 → 200)** per direct feedback that knockdown was coming off one big hit or a single melee combo too easily. Scaling the pool uniformly doubles the hit count to knock down for every weapon at once without hand-retuning each part's `enduranceDamage`.

### 9.6 Known gaps from this pass

- The controller legend panel's visual fade-in still hasn't been screenshotted (see §8's note — same backgrounded-tab limitation, now also confirmed to affect `getComputedStyle`, not just `requestAnimationFrame`). The underlying class-toggle logic is verified correct.
- AI shield-engagement (`DummyAI`) uses a simple randomized hold/release timer gated by proximity, not a reaction to what the player is actually doing (the AI has no visibility into the player's current action, only position) — reasonable but unsophisticated.
- Melee weapon meshes (saber/hammer/daggers) are static, always-visible attachments; they don't currently animate distinctly from the existing generic swing-arc VFX in `Melee.ts`.

## 10. Chassis Color Identity + Original Custom Robo Control Scheme (2026-07-13)

### 10.1 Each chassis now has its own paint identity

`RoboMesh.ts` previously colored every chassis identically (hull/accent/joint all came straight from the passed-in team colors), so the only visual distinction between chassis was silhouette. Added a `CHASSIS_PALETTE` (Vanguard: steel blue-grey / Skylance: pale aerospace silver / Wraith: dark violet-black / Bulwark: bronze-olive) that now drives the hull and joint materials directly, keyed off `loadout.body.id`. Team identity (player cyan vs. enemy orange) is preserved two ways: the accent glow (visor, chest core, weapon highlights) is still 100% the passed-in team color, and the hull/joint are lightly tinted (22%/15%) toward the team color so the two robos stay readable at a glance even when both are flying the same chassis. Verified visually via the hangar's live preview: all 4 chassis screenshotted with clearly distinct base colors while the cyan accent stayed consistent.

### 10.2 Gamepad remapped to the original Holosseum control scheme

Per the reference table (Stick=Move, A=Jump, B=Fire Gun, L=Fire Pod, R=Fire Bomb, X=Dash, Y=Switch Targets), with L/R read off the Xbox left/right triggers as directed. New mapping in `input.ts`:

| Button | Action |
|---|---|
| Left stick | Move |
| A (0) | Jump/hover, mash to recover, menu confirm |
| B (1) | Right arm: fire gun (held) / swing melee (pressed) |
| X (2) | Dash |
| Y (3) | Switch targets / lock-on toggle |
| LT (6) | Fire pod (deploy/recall) |
| RT (7) | Left arm: throw bomb (pressed) / hold shield (held) |
| Start (9) | Pause |
| D-pad | Menu navigation |

Since gun/melee and bomb/shield are mutually-exclusive loadout choices (§9.1) rather than always-on slots like the original game, B and RT each do double duty for whichever half of the pair is actually equipped — mirroring how LMB/RMB already worked on keyboard, the unequipped half's system just no-ops. This let the whole remap live entirely in `Input.ts`: `PlayerController` already read `fireHeld`/`meleePressed`/`held("KeyQ")`/`justPressed("KeyE")` etc. generically, so retargeting which physical button produces which merged key-code was sufficient — no combat-code changes needed. "Switch Targets" maps to the existing lock-on toggle, since a 1v1 duel makes them functionally the same action (the original table's own caveat, "if against more than one robo," concedes this).

Verified precisely, not just by inspection: mocked a fake Gamepad object and drove `Input.poll()` directly, confirming each button produces exactly the intended merged key-code (A→Space, X→ShiftLeft, Y→Tab, LT→KeyE, RT→KeyQ, Start→KeyP), that B correctly drives both a continuous `fireHeld` while held and an edge-triggered `meleePressed` only on the fresh-press frame (not on subsequent held frames), and that a gamepad Start press flows through the real `frame()` loop's `justPressed("KeyP")` check into an actual `togglePause()` state flip and back.

### 10.3 On-screen control text updated to match

`Hud.ts`'s `#hud-pad-legend` panel and the "DOWN — MASH SPACE" knockdown callout (now "DOWN — MASH SPACE / A", since A is no longer literally the "Space" key to a controller player) were updated to the new mapping. The keyboard-only `#hud-controls` hint, the pause menu, and the hangar's "Arrows/D-pad move · Enter/A select" nav hint were all left unchanged, since none of them reference a button that actually moved — keyboard bindings are untouched by this pass, and A still confirms in menus.

## 11. "Robo Controller Layout" Remap, Firing Momentum, Bomb Reticule, Rotating Camera (2026-07-14)

Direct follow-up to §10, superseding its gamepad mapping with a new one supplied verbatim by the user (`HOLOSSEUM_REFERENCE.md` "Robo Controller Layout"), plus three real gameplay-feel systems the reference implied: firing-affects-movement, a hold-to-aim bomb reticule, and a camera that rotates/zooms to frame both fighters instead of centering the player.

### 11.1 Gamepad remapped again: B↔LT swap

New table: A=Jump (unchanged), **B=Fire Pod** (was LT), **LT=right arm — fire gun (held) / swing melee (pressed)** (was B), RT=left arm — bomb/shield (unchanged), X=Dash (unchanged), Y=Switch Targets (unchanged), Start=Pause (unchanged). Mechanically this is a two-line change in `input.ts`: `BUTTON_TO_CODE`'s `[6,"KeyE"]` became `[1,"KeyE"]`, and the specially-handled dual-purpose `BUTTON_RIGHT_ARM` constant moved from `1` to `6`. Everything downstream (`PlayerController`, `Hud`'s pad legend) reads the same merged key-codes as before, so nothing else needed to change for the swap itself.

**A+A (double-tap jump while airborne = air-dash/mobility move)**: new to this pass. `Input` now tracks a `performance.now()` timestamp per key-code on every fresh press (`registerPress`, replacing the old bare `pressed.add`) and exposes `wasDoubleTapped(code)`, true for one frame if a second press lands within 300ms of the last. `PlayerController` ORs this into the existing dash trigger: `dashRequested = justPressed("ShiftLeft") || (!grounded && wasDoubleTapped("Space"))`. This reuses the *entire* existing per-chassis dash system (normal/long/vanish — already a mobility move that varies by chassis) rather than inventing a separate "air-dash/teleport/double-jump" mechanic from scratch, since the existing dash profiles already cover that spread of feel.

### 11.2 Firing affects movement: slide on the ground, halt in the air

Per the reference ("firing while moving usually causes your Robo to slide... firing in the air temporarily halts your momentum"), `RoboIntent` gained `firingGun` (true only when the right arm is a gun and actively firing — melee already has its own fully-committal lunge/swing/recovery state machine via `actionLock`, so it was left alone; "same is true generally with melee weapons depending on which one it is" already covers that variability). In `Robo.update()`'s movement resolution: grounded + firing blends toward the desired velocity with a slow correction (`fireSlideCorrection`, reusing the exact `onIce` momentum-retention pattern) instead of snapping instantly; airborne + firing decays horizontal drift toward zero at a much faster rate (`fireAirHaltRate`) instead of the normal air-steering accel. Verified by direct velocity sampling: grounded-non-firing snaps a perpendicular direction change to exactly 0 in one step, grounded-firing decays gradually (7.2 → 6.82 → 6.45 while the new axis ramps 0 → 0.38 → 0.75); airborne-firing decays 6.64 → 5.65 → 4.80 → 4.08 (~15%/step, matching the 9/s rate).

### 11.3 Bomb reticule: hold to aim, release to throw

`Bomb.tryThrow()` (instant, press-once) is gone, replaced with `startAim()` / `updateAim()` / `release()` / `cancelAim()`. Holding the left-arm-bomb input opens the reticule and keeps it live-tracking every frame; releasing deploys the bomb wherever the reticule currently sits (superseding the old two-pass lead-prediction hack — continuous live tracking is strictly better). `BombPart` gained `reticuleAnchor: "target" | "self"` and `reticuleRange: number` per the reference's Charge-Bomb-vs-Smash-Bomb distinction: **Impact Bomb** tracks the enemy (`"target"`, range 20 — effectively anywhere in the arena), **Quake Bomb** is fixed just ahead of your own robo (`"self"`, range 4 — a close-range, high-commitment slam that literally cannot be aimed further out). Not implemented: a Winder-Bomb-style "fires while the reticule is out" continuous variant — neither current bomb needs it, so the field was left out rather than added speculatively; it's a natural extension point when a third bomb type wants that behavior. `DummyAI` mimics a hold gesture (start aim, hold 0.25–0.55s, release) rather than instant-firing.

A new `#hud-bomb-reticule` DOM element (amber diamond, distinct from the red/green lock-on circle) tracks `Bomb.aimPoint` via the same `Vector3.project` technique as the existing lock-on reticle, shown only while `aiming` is true.

### 11.4 Bomb/shield now instantly halt all momentum, even mid-air

Per the reference ("This secondary weapon instantly halts all momentum, even mid-air. This is true for the shield as well as bomb"), both now drive a single `intent.leftArmActive` flag (true while shield is held, or while a bomb is being aimed) that Robo's movement resolution checks *before* the grounded/air branches: while active, horizontal velocity is hard-set to exactly 0 every frame, no decay curve, and the dash trigger is gated off entirely (`!leftArmActive`, replacing the old shield-only "no air-dashing" guard — now dashing is blocked outright, grounded or airborne, matching "vulnerable while aiming"). This is a strictly stronger effect than the previous shield mechanic, so `ShieldPart.moveSpeedMult` (a fractional ground-speed multiplier) became dead weight and was deleted along with its two catalog values and the `shieldActive`/`shieldMoveMult` locals that computed it. Verified: velocity hard-zeroes in one step from any prior value, even mid-air with active move input; a dash attempt while active leaves `boost` completely unchanged (blocked, not just failed to trigger).

### 11.5 Camera: rotates and zooms to frame both fighters, player off-center

Per the reference ("camera... automatically rotates to keep both your and your opponent's robot in sight... your robo shouldn't be centered... but should still be clearly in view"), `PlayerController.updateCamera()` was rewritten from a player-anchored fixed-offset camera to one that: computes the horizontal separation vector between the two robos, derives a view direction *perpendicular* to it (so the fighters always read left-right on screen regardless of which way the arena axis they're actually separated along), and smoothly rotates toward that direction each frame (continuity-guarded — picks whichever of the two valid perpendiculars is closer to the current direction, so the camera eases instead of flipping 180° when the fighters' relative position crosses an axis). The look-at point is a `targetBias`-weighted blend between the player and the enemy (0.4 — biased toward the player so they stay legible, not a true 50/50 midpoint) rather than the old "player position + small nudge toward enemy." This was safe to do *because* movement was already derived from the camera's live quaternion every frame (§8's inverted-controls fix) — a rotating camera can't desync WASD/stick mapping.

Because the camera is `THREE.OrthographicCamera`, "zoom" can't be done by moving the camera farther away (orthographic projection ignores distance) — it has to resize the frustum. `updateCamera()` now recomputes `top`/`bottom`/`left`/`right` every frame from a target frustum height that grows once fighter separation exceeds `zoomStartDistance` (12m), scaling up to `frustumSize × (1 + zoomMax)` at `zoomStartDistance + zoomRange` apart, smoothed at the same rate as camera position.

Verified precisely: with fighters separated along Z (the spawn-line default), the camera settles at `x=-8` (offset perpendicular to Z, i.e. along X) — a side-on isometric read matching the reference screenshots. Re-run with fighters separated along X instead, the camera settles at `z=-8` (offset swapped to the other axis) — confirmed the rotation actually re-derives per fight geometry rather than being hardcoded. Frustum height measured exactly 20 (base) at 2m separation and exactly 34 (`20×1.7`, the configured max) at 30m separation, matching the zoom formula exactly. A rendered screenshot with fighters on the spawn line confirms both robots clearly visible and the player robot legibly off-center rather than screen-centered.

Removed `TUNING.camera.lookAhead` (dead once the look-at point became midpoint-based instead of "player position + forward nudge").

## 12. True 45° Isometric Pitch, B↔LT Swap-Back, Manual Aim Steering (2026-07-14, later same-day session)

Direct follow-up to §11: the camera was rotating/zooming correctly but still read as too top-down, the gamepad mapping needed the right/left arm and pod buttons swapped back, and the bomb/pod holds needed real stick-driven aim control instead of pure auto-tracking.

### 12.1 Camera pitch: 45-45 isometric rule

`TUNING.camera.height`/`back` were `24`/`8` (≈71.5° from horizontal — nearly a top-down look). Per the classic isometric-camera guideline the user linked (height == back == a 45° look-down angle, with the ground plane reading as visibly tilted rather than flat), both are now `16`/`16` — an exact 45°. Nothing else about the camera's rotate-to-frame-both-fighters or dynamic-zoom behavior from §11.5 changed; this was purely the pitch constant. Verified: `camera.position` settles with `|x| === |y|` (16/16) when offset along X, confirming the exact angle; a rendered screenshot shows crates with clearly visible top *and* front faces (vs. the old near-flat top-down read) and both robos legibly in a 3D volume rather than as flat top-down sprites.

### 12.2 Gamepad B↔LT swapped back

The user's newest reference reorders the mapping again: **B = Fire Gun/Melee** (right arm), **L = Fire Pod** — the reverse of §11.1's B=Pod/LT=Gun-Melee. This is the same two-line change as §11.1, just undone: `BUTTON_TO_CODE`'s `[1,"KeyE"]` reverted to `[6,"KeyE"]`, and `BUTTON_RIGHT_ARM` reverted from `6` back to `1`. RT (bomb/shield), X (dash), Y (lock-on), Start (pause) were all already correct and untouched.

### 12.3 Manual aim steering: pod launch direction, bomb reticule offset

Both bomb and pod holds now accept stick input to manually influence their aim, on top of (bomb) or instead of (pod) their existing auto-aim, using the exact same `move` vector `PlayerController` already derives from stick-or-WASD for movement — meaning this works identically on keyboard (WASD) and gamepad (left stick) for free, and required no new input-reading code.

- **Pod** (`Pod.steerAim(dir, dt)` / `clearAim()`): holding the pod input (E/L) while it's deployed turns a manual `aimDir` toward the stick's direction at a limited rate (`aimSteer.podTurnRate`, 4 rad/s — a turret-like gradual swing, not a snap), used as the shot's *launch heading* the next time it auto-fires. Releasing the pod input clears `aimDir` back to `null`, reverting to auto-aim-at-the-enemy. Since `Projectiles.update()`'s homing re-derives the target every frame regardless of initial heading (`target = owner === player ? enemy : player`, not tied to the spawn aim point), manual aim only changes the shot's initial curve-in path, not its ultimate destination -- a tactical nudge, not a full manual-fire override, matching the user's own "in case that matters" hedge. Since holding the pod input repurposes the movement stick, movement is frozen for that duration (`moveDir` zeroed) — you can't steer the pod and walk at the same time with one stick, which was true either way once the same stick does both.
- **Bomb** (`Bomb.steerAim(dir, dt)`): holding the bomb input already halts movement entirely (§11.4's `leftArmActive`), so redirecting the same stick to the reticule was free. Adds a persistent `manualOffset` (velocity-integrated at `aimSteer.bombOffsetSpeed`, 6 m/s, reset every fresh `startAim()`) on top of the anchor-computed base point (enemy-tracking or fixed-ahead, per §11.3), with the *combined* point's distance from the robo clamped to the bomb's own `reticuleRange` — steering can nudge the aim within the weapon's envelope but never past it.

Verified precisely: pod `aimDir` locks fully onto a steered direction after sustained input, then turns gradually (not snaps) toward a new direction over subsequent frames, and clears to `null` on release. Bomb reticule, steered hard in one direction for 5 seconds of simulated input, settled at exactly `reticuleRange` (20m for Impact Bomb) from the robo — confirmed the clamp holds even under sustained maximum steering.

### 12.4 On-screen text updated again

`Hud.ts`'s pad legend now shows `B: Gun / Melee`, `LT: Pod (hold: steer)`, `RT: Bomb (hold: steer) / Shield`, and the left-stick row now reads "Move / steer aim". The keyboard `#hud-controls` hint gained "(WASD steers)" notes on the Q and E entries. `PlayerController`'s and `input.ts`'s header comments were updated to match the restored B/LT mapping and document the new steering behavior.

## 13. Parts, Holosseums & Weapon Damage Research Pass (2026-07-14, planning only)

Research-and-documentation pass, no code changes. The user supplied four Custom Robo (GameCube) strategy-guide sources — the instruction manual, a Holosseum FAQ covering all 35 named arenas, a Parts FAQ covering ~150 named parts across 5 slots, and a Weapon Damage Guide with the game's actual damage/multiplier tables — with the instruction to analyze everything about parts, arenas, and weapon damage and fold it into our design docs, renaming everything to our own vision rather than reproducing the source's names or copyrighted text.

**[`PARTS_AND_DAMAGE_REFERENCE.md`](PARTS_AND_DAMAGE_REFERENCE.md)** (new doc) maps the source's ~150 named parts down to every genuinely distinct mechanical archetype (not a 1:1 port — many source parts are mirrored left/right pairs or incremental tier reskins of the same idea) and gives each archetype an original name in our voice, cross-referenced against which are already covered by our existing catalog (`Vanguard`, `Blaster`, `Impact Bomb`, etc.) versus net-new expansion targets: 3 new body archetypes (`Halcyon`, `Corsair`, `Juggernaut`), 7 new guns (`Trident`, `Arcjet`, `Snare Beam`, `Undertow`, `Farlight`, `Jolt Caster`, `Locust Swarm`), 6 new bombs (`Barricade Charge`, `Sidewinder`, `Sapper Mine`, `Cryo Canister`), 3 new pods (`Skitter Pod`, `Glacier Pod`, `Veil Pod`), and 3 new legs (`Stilt Legs`, `Vector Legs`, `Plume Legs`). Also documents two structural ideas as future systems rather than named parts: a per-bomb selectable **impact type** (the source's letter-suffix knockback system) and **damage reduction while a target is downed** (the source's 30% rule — a real mechanic we don't have yet, worth prototyping in `Health.ts` since it caps how far a single knockdown opening can snowball). Confirms our existing `atkMult`/`defMult` damage-formula shape already matches the source's own body-offense/body-defense multiplier model, including the "offense should cluster tighter than defense" spread pattern.

**[`HOLOSSEUM_REFERENCE.md`](HOLOSSEUM_REFERENCE.md)** gained a new §3 covering five patterns the original 14-stage survey didn't have an example of: sloped/inclined terrain (**the Basin**), a shrinking safe zone that closes over the course of a fight (**the Closing Vein**), a floor that splits and shifts mid-fight (**the Sunder**), a pure perception/camera-distortion hazard (flagged as higher-risk for us specifically, since our camera already does real legibility work per §11.5), and wall density as its own spectrum independent of arena size, down to a completely open no-walls extreme (**the Open Void**). Also notes a structural confirmation: several source stages are explicitly the same base wall layout with a hazard overlay applied on top, which is exactly the shape of our own `Arena.ts`/`rollArena()` layout×hazard system — validation of an existing decision, not a new idea to adopt.

None of the new parts or arena patterns above have stats, meshes, or catalog entries yet — this was scoped as a research/planning pass per the user's explicit "it will all be implemented in the game soon" framing, not an implementation one. A fifth source (a 3D-model-rip gallery of the original game's robo/part/arena assets) was checked for visual silhouette reference but yielded only asset category counts, no extractable descriptions — noted in `PARTS_AND_DAMAGE_REFERENCE.md`'s sourcing as visual-reference-only, same treatment `ROBOT_SHELL_DESIGN.md` already gives its own Fandom-wiki image references.

## 14. Warband Theme: Historical Warrior Cultures + Catalog Rename + First Mesh Redesign (2026-07-14)

Follow-up to §13: the user asked whether our chassis could visually resemble specific named Custom Robo characters (Ray 01, Seeker, Javelin) using the 3D-model gallery as reference. Declined that specific framing — recreating a *named, copyrighted character's design* crosses from inspired-by into replica-of, which is exactly the line `ROBOT_SHELL_DESIGN.md` §2 already drew ("no shell should be a direct copy of a named Custom Robo"). The user's follow-up redirected productively: use the source's general silhouette *proportions* as a starting scaffold, but drive the actual final look with an original theme — historical warrior cultures and armies across history, Gundam/Armored-Core mecha art direction. This is both a stronger creative identity than "generic sci-fi robot" and the safest possible way to differentiate from the source material.

**[`WARBAND_THEME_REFERENCE.md`](WARBAND_THEME_REFERENCE.md)** (new doc) is the canonical theme bible. Maps all 7 body archetypes (4 implemented + 3 documented in `PARTS_AND_DAMAGE_REFERENCE.md` §2) to historical warrior cultures chosen for *mechanical* fit, not just aesthetics: Vanguard→**Legionnaire** (Rome, disciplined all-rounder), Bulwark→**Crusader Knight** (immovable tank), Skylance→**Valkyrie** (Norse, winged glass-cannon flier), Wraith→**Shinobi** (Japan, vanish-dash evader), and three not-yet-built archetypes: Corsair→**Pirate/Privateer** (the name already means this), Halcyon→**Aztec Eagle Warrior** (air mobility specialist), Juggernaut→**Mongol Kheshig** (heavy *and* mobile — the Mongol military's actual historical reputation). Also renames every part family from `PARTS_AND_DAMAGE_REFERENCE.md` using real historical weapons/equipment pulled broadly across cultures rather than bundled per-chassis, since loadouts mix and match freely (a Shinobi can carry a Roman Ballista) — e.g. guns draw from the English longbow, Chinese repeating crossbow, Roman siege ballista, Korean multi-rocket Hwacha; bombs from Byzantine Greek fire, Chinese gunpowder bombs, Aboriginal boomerangs; pods from China's Terracotta Army, war-trained dogs, Greek Gorgon myth. Also proposes renamed Holosseum concepts tying `HOLOSSEUM_REFERENCE.md`'s hazard patterns to specific historical sites (The Colosseum, Thermopylae Pass for the shrinking-safe-zone lever, Temple Steps for sloped terrain, etc.).

**Catalog renamed** (`src/parts/parts.ts`): every currently-implemented body/gun/melee/bomb/pod/leg's *display* `name`/`blurb` was updated to the new theme — `.id` fields were deliberately left untouched (still `"vanguard"`, `"blaster"`, etc.) so existing `localStorage` saves (`loadLoadout()`) keep working; this is a pure display-text change, zero gameplay/stat impact. `Aegis Barrier` and `Bastion Plate` (shields) needed no renaming — Greek myth and a real fortification term already fit. `Saber` and `Warhammer` (melee) likewise already fit and were kept.

**First mesh redesign, verified**: `RoboMesh.ts`'s `buildVanguardChassis()` (Legionnaire) got a real detail pass — banded lorica-segmentata-style torso plating (two raised seam strips), a tall rectangular chest panel echoing a scutum shield, a helmet brow rim, a transverse centurion-style crest, hanging pteruges strips at the waist, and stepped pauldron caps over the shoulders, all still built from the same `box()`/`cyl()` primitive helpers — going from 9 primitives to 19 for the chassis alone. Verified two ways: mesh-count assertion through `window.hangarPreview` (9→19 chassis primitives measured exactly as predicted, confirming the new code was actually rendering and not stale-cached), and rendered screenshots at both a normal hangar-preview framing and a close-up on the head, confirming the crest/brow-rim/visor read clearly as a helmet and the banding seams and pauldron caps read as genuine armor layering rather than a flat box.

**Real bug caught mid-verification, not a code bug**: the first screenshot attempt showed the *old* flat design despite the file, the dev server's serving, and Vite's HMR log all confirming the new code was live — root cause was a stale `localStorage` saved loadout from earlier this session's testing with `body: "bulwark"` selected, so the hangar preview was correctly rendering the *unmodified* Crusader Knight chassis, not a stale Legionnaire. Cleared via `localStorage.removeItem(...)` + reload; the mesh-count check (9 vs. 19 primitives) is what surfaced the mismatch and is now a good habit for future "did the file I just edited actually render" checks, faster than eyeballing a screenshot.

**Scoped deliberately to one chassis this pass** (`CLAUDE.md`'s "small testable increments" rule) — Valkyrie/Shinobi/Crusader Knight are documented with a redesign direction in `WARBAND_THEME_REFERENCE.md` §5 but not yet built, pending confirmation the Legionnaire direction is the right one before repeating it three more times. Individual weapon/pod/leg mesh redesigns (as opposed to chassis) are noted as a further follow-up, not attempted this pass.

## 15. Reference-Model-Informed Detail Pass on the Remaining Three Chassis (2026-07-14)

Direct follow-up to §14, confirmed by the user's "this looks great so far." The user provided four private 3D reference assets (OBJ meshes + PNG texture atlases) and asked for the four chassis to draw on them as abstract reference, mapped by role to Legionnaire, Crusader Knight, Valkyrie, and Shinobi.

**Same copyright line as §14, held**: these are actual copyrighted game assets (not just a webpage), so they were used only as local reference for extracting *abstract* proportion and color-language data to inform genuinely original geometry — never imported, traced, or embedded into the project. The extracted zips were deleted from the scratchpad after use; nothing from them entered the repo.

**No 3D renderer was available** (Blender MCP checked, not running/connected) — worked around this with two techniques instead of eyeballing: (1) viewed each model's "Original" texture atlas directly (Read tool renders PNGs) for color palette and panel-line/insignia language — knight: navy/steel/gold with curved decal language; flier: pink-magenta/gold/silver with sharp layered blade shapes; legionnaire: red/white/blue angular blades; shinobi: green/white/red rounded-hex plates. (2) Wrote a small Node script (`profile.js`, scratchpad-only) that parses each `.obj`'s raw vertex data directly, computes the overall bounding box, and bins vertices into 12 horizontal height-bands to get a width/depth profile — a coarse silhouette read without needing an actual render.

That profile data produced real, sometimes counter-intuitive findings that directly shaped the redesigns: the knight reference is wide *even excluding its T-pose arm-spread band*, and is the tallest of the four (h/w=0.76, height 3.91) — confirmed the knight chassis should read as taller *and* bulkier, not just wider. The flier reference has a distinctly narrow waist band low down before widening again toward the shoulders (h/w=1.39, the tallest/slenderest ratio) — directly justified adding a genuine cinched-waist silhouette rather than a uniform torso. The shinobi reference is the *shortest* of the four in absolute height and has the widest h/w ratio (0.70) — a low, compact read, not the tall-willowy "ninja" silhouette a name alone might suggest, which validated keeping Wraith's already-documented "shorter, angular" identity (`ROBOT_SHELL_DESIGN.md` §2) rather than second-guessing it.

**Three more chassis got the same additive-detail treatment as the Legionnaire** (new primitives layered onto the existing skeleton, anchors/attach-points untouched, so weapons/legs/backpacks keep working unmodified):
- **Valkyrie** (`buildSkylanceChassis`): a cinched accent-colored waist belt, a chest crest trim, a head crest spike, and a second, smaller pair of wing-blades layered behind the existing primary sweptback fins for a more layered plumage read.
- **Shinobi** (`buildWraithChassis`): a hood/cowl draped over the crown replacing the plain boxy head silhouette, a diagonal wrap-sash across the torso, and a longer/lower trailing cloth panel — kept strictly to `hull`/`joint` materials only, no `accent`, preserving the chassis's deliberate "minimal glow, built to not be seen" identity from the original design.
- **Crusader Knight** (`buildBulwarkChassis`): a two-piece accent-colored heraldic cross emblem on the chest (a vertical bar plus a crossbar, positioned per classic cross proportions), a great-helm brow band, a mail-hem trim at the waist, and stepped pauldron caps matching the Legionnaire's shoulder treatment.

**Verified with rendered screenshots of all four chassis together** (not just one at a time) after clearing the same stale-`localStorage` trap §14 hit: each is now clearly visually distinct at a glance — the Crusader Knight's cross emblem in particular reads as an unambiguous, well-composed heraldic symbol rather than a busy shape. `npx tsc --noEmit` and `npm run build` both clean throughout.

## 16. World Theme Pivot: Neo-Feudal Used Future (2026-07-15)

**Supersedes §14's Warband theme.** New canonical world/theme doc: [`SETTING_AND_FACTIONS.md`](SETTING_AND_FACTIONS.md) — read it in full; `WARBAND_THEME_REFERENCE.md` is kept only as historical record of how the chassis silhouettes/meshes were built (still valid, no rework needed).

The setting is now explicit: a cataclysm ("the Long Hush") nearly ended humanity hundreds/thousands of years ago, for reasons no surviving faction can fully confirm (`Cataclysm Backstory`, `From Cataclysm to Myth`). Survivors rebuilt from tribes to clans to nations, arriving at a `Feudal Future` — Great Houses, chartered Knightly/Military Orders, and mercenary free companies, all fielding inherited ancestral war-mechs nobody currently alive knows how to build from scratch (`Lost Technology`, `Low Culture, High Tech`). This directly motivates the visual split between well-kept Order-issue chassis and scavenged, patchwork non-Order chassis (`Used Future`) as an in-fiction fact rather than an art-style choice, and it gives §2.2's knockdown/rebirth cycle a diegetic origin: an unexplained ancestral failsafe nobody can replicate or fully explain, only trigger — the literal "Rebirth Protocol."

Each existing/planned chassis now maps to a proposed in-fiction Order (real 1100–1500s military orders and other feudal societies as inspiration, never a literal copy) instead of a single named Earth culture — see the faction table in the new doc. Two chassis were nudged to stay inside that timeframe while keeping their built silhouette: Vanguard (was Legionnaire/Rome) → Byzantine-inspired **Aureate Legion**; Skylance (was Valkyrie/Norse) → Baltic-Crusades-inspired **Order of the Winter Wing**. The other five slot in with less change. All of this is flagged as draft naming pending confirmation, not finalized canon.

## 17. Unity Stage 1 Playable Duel Slice (2026-07-15)

First playable Unity build: the full §2.2 twin-bar loop, boost-economy movement (§3.3), homing gun, 3-hit melee string with gap-closer, dummy AI, and the settled §12 camera, in one sealed arena with crate cover. Domain rules live in plain-C# `RebirthProtocol.Domain` classes (`CombatantHealth`, `BoostGauge`, `GunCycle`, `MeleeAction`, constants in `CombatTuning` mirroring the prototype's `tuning.ts`) with EditMode tests; MonoBehaviours in `RebirthProtocol.Battle` are thin glue, all driven from `DuelManager.Update` in a fixed order so simulation never depends on Unity script ordering. The Duel scene contains a single bootstrap object — everything spawns from code.

Stage-1 judgment calls (flagged, not silent): melee is on its own button (K / gamepad Y) instead of sharing B with the gun, since the right-arm gun-XOR-melee loadout mutex doesn't exist yet — both weapons are equipped simultaneously in this slice, matching the prototype's own Stage 1; lock-on is automatic (one enemy, always locked), so Y's eventual lock-on/switch-targets role is unclaimed for now; crates are indestructible cover (prototype had 3-hit destructible crates — port later); double-tap-A air-dash, shields, bombs, pods, ice, and per-part stats are later stages. Robos are primitive capsule bodies with team-colored accents — the Cobalt Knight asset integration comes with the animation pass.

One real port bug found by booting the actual build (editor tests can't catch it): runtime-created materials can only use shaders that shipped in the build, and URP/Unlit gets stripped when no asset references it — `Shader.Find` returned null in the player and every projectile spawn threw. Lesson recorded in `BattleMaterials`: always fall back to URP/Lit (guaranteed present via the default primitive material), and boot-check the real player build, not just PlayMode tests.

## 18. Unity Stage 2 Slice: Five-Slot Loadout + Shields, Bombs, Pods, Hangar (2026-07-15)

The full §2.1 loadout system, ported from `parts.ts` with every number intact: 4 bodies / 3 guns / 3 melee / 2 bombs / 2 shields / 2 pods / 3 legs, gun-XOR-melee right arm, bomb-XOR-shield left arm, `PartsCatalog.ComputeStats` flattening body×legs multipliers into the stats the avatar runs on, per-archetype dash profiles (normal/long/vanish — vanish grants real projectile intangibility). Shields per §3.2: hold-to-raise, rooted while up (hard momentum halt, dashing blocked), front/back block %, chip always lands, blocked damage drains shield HP, guard-break feeds the SAME knockdown state, melee-parry drains the attacker's endurance. Bombs are hold-to-aim/release-to-throw with a live tracking reticule (target-anchored Greek Fire Pot, self-anchored Zhen Tian Lei); AoE hits both robos — your own bomb can knock you down. Pods run on their own energy pool and stay deployed. A code-built hangar screen opens before each duel (W/S slot, A/D part, Enter/A deploy), the loadout persists via PlayerPrefs, and the enemy rotates through three preset builds per launch/rematch so tradeoffs get tested against variety. `-autodeploy` command-line flag skips the hangar for automated smoke tests.

Deferred from the prototype, flagged not silent: manual reticule/pod aim steering (stick redirect while holding LT/RT), melee-lunge chaining visuals per weapon shape, Winder-style continuous bombs, positional audio — all polish-pass items, none load-bearing for the loadout system itself.

## 19. Unity Parity Pass: Visuals, Audio, Arenas, UX, Hangar Preview (2026-07-16)

Five-task pass closing most of the remaining prototype-parity gaps (the roguelite run layer stays on hold by explicit decision):

1. **Per-loadout visuals** (`RoboVisual`): data-driven primitive builds — four distinct chassis silhouettes with the prototype's `CHASSIS_PALETTE` hull/joint colors tinted toward team color, per-gun/melee/shield/bomb/legs attachments, Wraith's no-glow identity preserved, shield plate that visibly swings forward while held. This is the bridge until the rigged Blender mechs land with the animation pass.
2. **Audio**: the prototype's entire synthesized soundscape, ported recipe-for-recipe — `SynthClips` bakes tones (exponential freq/gain ramps) and filtered noise to PCM AudioClips; `SfxPlayer` carries all ~24 combat/UI sounds from `sfx.ts`; `MusicSequencer` is the lookahead step-sequencer from `music.ts` rebuilt on `AudioSettings.dspTime`/`PlayScheduled` — 84 BPM hangar pad and 128 BPM combat loop over the shared Am-F-C-G progression, switching at hangar/deploy/duel-end.
3. **Arena variety** (`ArenaBuilder`): three layouts rotating per launch/rematch with the enemy build — Depot (crate field), Colonnade (tall pillars), Frostfield (central ice sheet: momentum carries, steering becomes a slow correction — the prototype's `onIce` rule). Crates are now destructible (3 gun hits, or destroyed outright inside a bomb blast).
4. **Control/UX parity**: pause (P/Start), red/grey lock-on diamond over the enemy (grey = currently invulnerable), gamepad legend panel while a controller is connected, double-tap-A air dash (same dash system X uses, per §11), and the previously-deferred manual aim steering — stick nudges the bomb reticule (clamped to reticule range) and turns the pod's launch heading at the prototype's rates.
5. **Hangar 3D preview**: live robo on a turntable dais at arena center, rebuilt on every part pick, framed by a perspective close-up camera while the hangar is open.

Still open after this pass: rigged mech models + animation (Cobalt Knight et al.), the roguelite run layer (§18 note stands), lava-style damaging hazards, positional audio, and the three unbuilt chassis archetypes.

## 20. Damaging Hazard Arena + Positional Audio (2026-07-16)

Two small, low-risk items closed from §19's remaining list (deliberately chosen over starting the bigger rigged-mech-integration work, which stays open pending a dedicated pass):

- **Cinderfield** (`ArenaBuilder`, 4th rotating layout): three lava pools at the prototype's exact positions/radii (`Arena.ts`'s `poolDefs`), dealing continuous DoT (24 HP/s, 14 endurance/s) to anyone grounded inside — ported straight from `applyHazards`, including that it bypasses shields entirely by hitting `CombatantHealth.TakeHit` directly instead of routing through `RoboAvatar.ReceiveHit` (environmental damage, not directional). Sizzle SFX gated by the same 0.4s per-robo cooldown as the source. `DuelManager.ForceArenaLayout(int)` exists so PlayMode tests aren't at the mercy of the static per-launch layout rotation.
- **Positional audio**: every combat/environmental sound in `SfxPlayer` now takes a world position and plays through a 3D-spatialized `AudioSource` pool (linear rolloff, 4–40m range matching the arena's scale); UI/run cues (menu clicks, victory/defeat stingers) stay on a separate flat 2D source since they're player feedback, not something happening in the world.

Still open: rigged mech models + animation, the roguelite run layer, the three unbuilt chassis archetypes.

## 21. Cobalt Knight Integration: Real Rigged Bulwark Visual (2026-07-17)

The Bulwark chassis now uses Codex's actual rigged Cobalt Knight mesh (`KnightRobot_Body.prefab`) instead of primitives — no animation yet (none exists to wire up), a static-mesh visual upgrade only. Real anchor points, not guessed ones: `Assets/RebirthProtocol/Editor/AssetHierarchyDumper.cs` (one-off, run via `-executeMethod`) dumped the prefab's actual transform hierarchy, which turned up all ten `Socket_*` empties documented in `KNIGHT_ROBOT_ASSET.md` (nested under an armature/bone hierarchy, not flat children) with real world coordinates — `CobaltKnight.cs` positions the gun/melee/bomb/shield/leg-thruster attachments at those, not hand-tuned guesses. The model's own forward is -Z where ours is +Z, and its right arm sits on -X where our convention is +X; a single 180° Y rotation on the wrapper transform fixes both at once, so every attachment is parented under that same wrapper and inherits the flip instead of needing manually mirrored coordinates.

Two integration problems worth remembering:
- **The Duel scene builds everything from code, so nothing referenced the prefab from an included asset/scene — it would have been silently stripped from a player build.** `Assets/RebirthProtocol/Editor/CobaltKnightResourceLinker.cs` copies it into `Assets/RebirthProtocol/Resources/Mechs/CobaltKnightBody.prefab` (via `AssetDatabase.CopyAsset`, run once via `-executeMethod`) so `Resources.Load` can find it at runtime. This is a duplicate, not a link — if Codex's pipeline regenerates the source prefab, the linker needs re-running to pick up the change.
- **A `-nographics` editor screenshot of the isolated asset renders solid grey** (forces a null GPU device, so `Camera.Render()` produces nothing) — don't waste time on that path for visual verification. What worked: booting the actual built player (real GPU rendering) with a temporary debug camera override for a close-up shot. At the tiny scale of the normal isometric duel camera, the real mesh initially looked like a squashed, unrecognizable blob in a screenshot — that turned out to be a distance/resolution artifact, not a bug; a close-up confirmed the model reads clearly (rivets, heraldic cross trim, shoulder pauldrons, all as documented). Don't judge a detailed mesh's correctness from a screenshot where it occupies ~100 pixels.

## 22. Controller-First Controls, Fullscreen, Expanded Audio, Combat VFX (2026-07-17)

Four bundled requests:

- **Controls remapped to the canonical Three.js controller layout** (§12): A = jump/hover (double-tap airborne = air-dash), X = dash, **B = right arm (gun held to fire OR melee pressed — one button, whichever the loadout equips)**, Y = lock-on/switch (a no-op tick with one opponent, kept for layout fidelity), LT = pod (toggle + steer), RT = left arm (bomb aim/shield hold), Start = pause. The only real behaviour change from the prior Unity mapping was folding melee off its own button onto B alongside the gun. Keyboard is retained as a 1:1 mirror (needed for headless testing and no-pad play) but the controller layout is canonical; HUD legend and hangar footer updated to match. **Controller feel is unverified in-engine — there is no physical gamepad in the dev environment (past pad work was mock-verified); the wiring is correct but needs a real-controller playtest.**
- **Fullscreen**: borderless-fullscreen-at-native was already the player-setting default, but a standalone player persists the last window mode to the registry, so earlier `-screen-fullscreen 0` test launches (and any windowed run) stuck it windowed on subsequent launches. `DuelManager.Awake` now forces `Screen.SetResolution(native, native, FullScreenWindow)` at runtime to override the persisted value — skipped when `-screen-fullscreen` is on the command line so the screenshot smoke tests still run windowed.
- **Audio**: richer layered explosion (sub-boom + crackle + debris crack + tail), a heavier metallic hit for chassis damage, a bright spark-tick for shots spraying off walls/crates, a backbeat snare, and three rotating combat chord progressions (A/E/D-minor) picked per fight so back-to-back duels don't all sound identical. Progression-dependent clip cache keys include the track id to avoid the name-collision-serves-wrong-pitch bug.
- **VFX** (`Runtime/Battle/Effects/`): `EffectsSystem` spawns world-space muzzle flashes, projectile-impact sparks (off robots, crates, and arena geometry), and layered explosions (flash → fireball → expanding ground shockwave disc → ember debris → rising smoke), plus trauma-style camera shake. `ScreenFx` adds a UI-based event flash, an animated film-grain overlay ("visual noise"), and a vignette. **Deliberate build-safety choice, don't "optimise" it away:** all world VFX are opaque scale-animated primitives, because the URP transparent-unlit variant is prone to being stripped from player builds (the Shader.Find-returns-null class of bug); screen-space transparency lives on the always-present UI shader instead. Verified via forced-explosion screenshots of the built player.

Also fixed in passing: a latent test-isolation bug, not a real gameplay issue. `DuelManager`'s shared `_enemyBuildIndex` static counter drives both the enemy's rotating loadout *and* the arena layout, and Cinderfield's lava pool 1 (`(-8, 3)`, radius 3) sits exactly on the default player spawn point `(-8, 0, 0)` — distance 3.0, right on the boundary. Any PlayMode test that didn't explicitly pin the arena layout and left the player at spawn could silently take a sliver of real lava damage depending on which layout the shared counter happened to rotate to that run, flaking exact-equality HP assertions. Every test that doesn't specifically want hazard behavior now calls `duel.ForceArenaLayout(0)` (Depot) to sidestep it — confirmed stable across repeated runs.

## 23. Unity Stage 3: The Roguelite Run Layer (2026-07-18)

The single largest deferred piece — §4's run structure, previously held back by explicit decision (§18/§19 notes) — is now built. A deploy starts a **5-fight run** against escalating rivals with **boon/spoils drafting between fights**, ported from the prototype's `run.ts`/`effects.ts`/`Draft.ts` with every number intact: flat ×(1 + 0.12·fight) enemy power mult on HP and ATK, player HP carried between fights (+15%-of-max heal, endurance/shield/boost reset), death ends the run, 1 reroll per run, skip allowed, 30% item drops from destroyed crates as walk-over pickups, all 10 boons and all 5 hyperbolic-stacking items. Domain logic (`Domain/Run/`: `RunState`, `RunOpponents`, `RunEffects`, `RunCatalog`, `DraftRoll`) is plain C# with EditMode tests; `DuelManager` orchestrates fights/drafts, and `DraftScreen` reuses the hangar's code-built-uGUI + keyboard/pad nav pattern. One seeded `System.Random` per run drives arena rolls, draft offers, and drop rolls (`DuelManager.RunSeedOverride` pins it for tests).

Judgment calls, flagged not silent:

- **Rivals are named Order pilots** per `SETTING_AND_FACTIONS.md` — Bannerlord Cassian (Aureate Legion) → Skald Maren (Winter Wing) → Vesk the Unseen (Umbral Concordat) → Warden Aldric → Grandmaster Otho (both Rust Cross). Same five escalating loadouts as the prototype (all-rounder → harasser → shielded melee rush → wall → kitchen-sink brawler). Names shown on the HUD ("FIGHT 2/5 — SKALD MAREN · ORDER OF THE WINTER WING") and the draft header; draft naming pending user confirmation like the rest of the faction table.
- **Spoils of War shipped with the draft** (the setting doc's proposed mechanic): each rival carries a signature arm (Longbow / Chu-Ko-Nu / Khopesh / Bastion Plate / Warhammer) offered as a 4th draft option alongside the 3 boons. Since the hangar already unlocks every part, its value is mid-run **adaptation** — the one way to change your locked loadout during a run, at the cost of a boon. Taking it respects the arm mutex (a melee spoil clears your gun, a shield spoil clears your bomb) and is **run-scoped**: the PlayerPrefs hangar loadout is untouched.
- **Arena rolls pick from the 4 authored layouts** (Depot/Colonnade/Frostfield/Cinderfield), fight 1 restricted to the hazard-free pair — a deliberate simplification of the prototype's orthogonal layout×hazard matrix (3×4). The full matrix wants its own arena-content pass (`HOLOSSEUM_REFERENCE.md` patterns) rather than a refactor of 4 layouts into 6 combos now.
- **Boons/items apply to the player only** (prototype rule kept): `RoboAvatar.Effects` is null for the enemy, every hook neutral-checks. Splinter darts spawn with `HitSource.None` so they can't chain themselves; Guard Crusher multiplies only the shield-pool drain, never the chip; afterimages are **opaque scale-fade primitives** per §22's URP transparent-variant stripping rule.
- **`_enemyBuildIndex` is gone**: enemy variety now comes from the run itself, which also retires §22's test-isolation footgun (arena still pinned via `ForceArenaLayout` in tests, now against the per-run seeded roll).
- **Mid-run defeat = run over, R = back to hangar** (scene reload, fresh run). The prototype pause menu's "restart fight" mitigation was not ported this pass — pause is resume-or-quit for now; worth revisiting if runs feel too punishing in playtests.
- **Draft/interlude visuals are minimal by design** this pass (text cards over the dimmed arena; victory beat is 1.4 s). A presentation pass (rival portraits/mech preview, run map) is future polish, not structure.

Verification: EditMode 54 (24 new), PlayMode 12 (3 new: victory→draft→advance with HP carry + power-multiplied rival, forced item drop walk-over pickup, death ends run). Windows dev build boots clean with `-autodeploy` (no exceptions in the player log). **Not yet user-playtested**: draft pacing, boon strengths, and the spoils-vs-boon tradeoff all need a real controller session — flagged per the no-gamepad environment constraint.

## 24. Full Theming Overhaul: Armory Canon, Mecha-Knight Art Direction, Arena Roster, Characters (2026-07-18)

Creative-direction pass folding the user's seven new source printouts (Parts FAQ, two map lists, Weapon Damage Guide, controls/mechanics FAQ, and the Robot Grand Battle tournament guide) into the neo-feudal used-future canon, plus a mecha-knight design-pillar research pass (Escaflowne / Knight's & Magic / Break Blade / 40k Imperial Knights / AC6 / Gundam IBO — user-directed via the Knights in Powered Armor trope family). Three new canonical docs, two updated, plus a display-name overhaul in code:

- **[`ARMORY_REFERENCE.md`](ARMORY_REFERENCE.md)** (new, canonical) — the naming system (relic-pattern / Hushforged / scrapwright tiers, slot vocabularies, no Earth-culture names), the full 55-part target roster with **melee and shield rosters at numeric parity with guns and bombs** (10/10 and 8/8, per user directive — every gun family has a designed melee counterpart), stat-bar language per slot (MIGHT/WARD/POISE/GAIT/WING etc. mapped onto existing sim fields), and the standing combat directives: **shield toll** (cooldown on lowering/breaking, like bombs) with per-shield **raise behavior** (Root / March / Air-hold / Air-drop), the six curated **tempers** (impact types distilled from the source's 11-letter blast system, incl. Unhorse and Fetter), and the **2-air-dash body cap** (extra dashes only from legs). §12 lists the 13 new sim capabilities the roster needs; §13 sequences the whole thing into passes A–J.
- **[`ART_DIRECTION.md`](ART_DIRECTION.md)** (new, canonical) — harness scale (10–14 m, chest cockpit, sensor-helm), the seven knight-read silhouette markers, the per-touchstone "how giant-knight media fights" survey, the seven-Order livery/heraldry table (generalizing the Cobalt Knight's cobalt/ivory/aurum), four material registers (Order-pristine → patchwork → scrapwright → Hushforged), fight-choreography direction (joust passes, **knockdown-as-kneel / rebirth-as-accolade**), VFX language (godlight gold vs. Hushforged violet, rose-window muzzles, toll-ring blasts — all within §22's opaque-primitive constraint), and audio direction (plainchant-leaning hangar pad, anvil/bell palette, the four-note "Hush motif").
- **[`ARENA_ROSTER.md`](ARENA_ROSTER.md)** (new, canonical) — the Lists: 19 named arenas mapping every distinct source stage/lever (the 4 built layouts renamed in canon: Depot→**the Tiltyard**, Colonnade→**the Broken Colonnade**, Frostfield→**Winterfield**, Cinderfield kept), in-fiction hazard framing (ancestral machinery nobody can turn off), home-List bias for rival Orders, and a six-batch rollout order starting with three geometry-only Lists.
- **[`SETTING_AND_FACTIONS.md`](SETTING_AND_FACTIONS.md)** (updated) — canon lexicon (**harness**, **armiger**, **the Lists**, a run = **a Passage of Arms**), the Grand Passage tournament culture with five future run modes mapped from the tournament guide (Laurels scoring, Vow of Temperance single-use runs, Shieldbrother 2v2, Trial by Ordeal handicaps, Twin Harness tag runs), the five rivals deepened into characters with doctrine and voice, and three future figures: the Herald of the Lists, Wrightmother Sella, and **the Riderless** (Hushforged pilotless harness — intended final boss, fought in the Cradle).
- **Code** (display names only, ids frozen; `Parts.cs`, `Run/RunCatalog.cs`, comment touch-ups): the 19 implemented parts renamed to armory canon (Longbow→Arbalest, Khopesh→Misericorde, Greek Fire Pot→Censer, Zhen Tian Lei→Anathema Charge, Aegis Barrier→Ward Veil, Bastion Plate→Pavise, Terracotta Sentinel→Iron Squire, etc. — full table in `ARMORY_REFERENCE.md` §11); chassis names now match their keeper Orders (Bannerman / Vesper / Duskmantle / Cobalt Knight — the last now name-aligned with its rigged asset); boons/items re-themed (Rearm Protocol→Rearming Rite, Leech Node→Victor's Chalice, Scrap Plating kept). **One gameplay change, per user directive: Duskmantle (wraith) DashCount 3→2** — no body exceeds 2 air dashes; Gryphon Greaves remain the only way to reach 3. `PartsCatalogTests` updated accordingly (4→3 in the stacking test).
- **Deliberately not done here**: no new parts/arenas/systems were implemented (this pass is canon + renames + the one dash-cap change); the rival pilot names were kept as-is and deepened rather than replaced; downed robos stay fully invulnerable (source's 30% rule noted, not adopted). Implementation proceeds via `ARMORY_REFERENCE.md` §13's pass list and `ARENA_ROSTER.md` §5's batch order.

## 25. Feature-Complete Content Canon: Armory v2, Combat Doctrine, Full Arena & Character Rosters (2026-07-18, second pass)

Direct follow-up to §24 on user direction: re-analyze all seven source printouts end-to-end (including the tournament guide's previously unread endgame — the Broken-Choir-analog arc, Bronze/Silver/Gold finals, battle-royale mode, hidden illegal-part caches, and the final duel against the player's own kin) and expand every section to **feature-complete**, with every source part/stage/mechanic mapped through the theme filter. Additional knight-mecha research folded in: Panzer World Galient (machines excavated from beneath a medieval world — our exact premise, 1984), Aura Battler Dunbine (the organic/summoned register → reserved for Hushforged), and 40k Imperial Knight heraldry / Freeblade rules (per-knight livery deltas; the Freeblade single-colour/personal-device/motto rule → the new Hedge Errantry's identity). Docs only — no code this pass.

- **[`ARMORY_REFERENCE.md`](ARMORY_REFERENCE.md) rewritten as v2, feature-complete**: 9 chassis patterns (two new: **Harrier** ← Little Raider, kept by the new **Hedge Errantry** faction; **Cockatrice** ← the source's odd glider, a masterless pattern) × the new **garniture** system (Field/War/Chase stat trims ← the source's Normal/Armor/Speed styles) = 25 relic bodies with full stat bars, offense/ward multipliers, and per-garniture charge attacks from the source's tables; **38 gun patterns each with a designed melee counterpart** (parity directive held at full scale), 18 bomb shapes ↔ 17 shields, 24 pods, 12 legs; Dexter/Sinister as the heraldic left/right variant convention; **tempers v2** (six directions + Branded modifier ← the source's 11 blast letters); a full **Hushforged tier** mapping every source illegal part to a hushed epithet with a drawback (the Rahu line = Shards/Voices/Hands/Shadows **of the Riderless**); and the **scrapwright tier redefined per user direction** — not a joke tier but the dependable human-built floor (Wrightsguild-made; high SOAK/POISE, no gimmicks, near-zero Burden, one "dependability perk" each, e.g. the Matchlock's rounds surviving your knockdown). New future system flagged: **Burden/Fitting** (relic parts strain a harness, scrapwright fits anything). Rollout re-sequenced into passes A–M.
- **[`COMBAT_DOCTRINE.md`](COMBAT_DOCTRINE.md)** (new, canonical): how battles work, distilled from the controls/tips FAQ and tournament guide for future combat passes — the **Casting** fight-opening ritual (cast relic-cores, mash to unfurl first), the four ranges, movement doctrine (circle strafe, slide shot, short-jump fire, landing risk, corner danger), the **overload rule** (a downed harness's gun rounds are wiped; Unhorse-temper downs don't trigger it; scrapwright rounds exempt), wait-first-strike-later rhythm, aimed-bomb vulnerability, per-garniture charge attacks (i-frames during, vulnerable around), team-battle rules (selective friendly fire, tag heal-floor and switch thresholds, 1v2 target-aggression model, 1v3 melee), the **mercy rule** (75/50/25% opt-in), **laurels scoring** (speed + vigor vs. task scores; −10% per rematch; **Hushforged parts halve laurels**; Class C→S armiger ranks), and eight named AI archetypes with one designed blind spot each.
- **[`ARENA_ROSTER.md`](ARENA_ROSTER.md) v2**: complete 35-stage coverage — 21 Lists with size/walls/motion/home-Order stats plus four formalized **overlays** (Emberpools/Rimefloor/Beltworks/Closing Vein ← the source's "Cell" pattern) and two story stages; the two "toy" stages rehabilitated by scale transposition (child's train → **the Pilgrim Road**'s walking ancestral freight-crawler; carousel → **the Reliquary Round**'s rotating saint-procession); Winterfield recognized as Tiltyard×Rimefloor.
- **[`SETTING_AND_FACTIONS.md`](SETTING_AND_FACTIONS.md)**: two faction rows added (Hedge Errantry, masterless Cockatrice) plus three institutions (**the Wrightsguild**, **the Litany Sisters**, and the antagonist **Broken Choir** — Hushforged traffickers who read the Long Hush as instruction); the Grand Passage expanded to the **feature-complete 15-event circuit** mapped from the source's full tournament ladder (Hearthside Tilt through the Golden Passage, with the Broken Choir's Gauntlets as the antagonist arc) plus a sixth run mode (**the Melee**, 1v3); a 21-armiger expansion roster with Order allegiances and doctrine (including Roald the Close, Brother Golias the all-Goliath jester, and the Litany Sisters), six named Broken Choir antagonists, and the **First Armiger** (the player's own master wielding Hushforged arms as the Golden Passage's final wall before the Cradle).
- **[`ART_DIRECTION.md`](ART_DIRECTION.md)**: three research entries added (Galient/Dunbine/Imperial-Knight-heraldry with the Freeblade rule), four livery rows (Hedge Errantry's one-colour-one-device-one-motto rule, Wrightsguild, Litany Sisters, Broken Choir), and the scrapwright material register rewritten to the dependable identity (boiler-plate, rivet rows, guild stencils, steam-not-godlight — beloved farm machinery among cathedral relics).
- **Codex's OilCan/GarbageBin/Binface meshes**: per user direction, no longer mapped to the scrapwright tier (they were Blender practice); tier mapping removed from the docs.
- **Nothing implemented in code this pass** — this is the content bible the implementation passes (ARMORY §12 A2–M, ARENA §5, DOCTRINE §10) now draw from.

## 26. Structure Directive: a JRPG-Shaped Game with a Western Soul + the Completeness Audit (2026-07-18, third pass)

**New structural canon, per user direction: the battle system is embedded in a JRPG-style game with strong western influences** — the shape of the source game's own story mode (a walkable world, named characters, tournaments entered from the world, customization between fights), told in our neo-feudal register (chivalric-romance arc, oaths and laurels, grounded dialogue) rather than an anime-urban one.

- **The shell**: a hub city — **Lasthold**, seat of the Grand Passage — with the hangar/test-yard, the Wrightsguild (repairs, scrapwright stock), stewardship shops, the Herald's hall (event board), and named armigers to talk to. Events on the circuit ladder (`SETTING_AND_FACTIONS.md`) are entered from the world; each event runs a Passage (the existing roguelite run layer) under its format rules. Story spine: the circuit → the Broken Choir arc → the First Armiger → the Cradle.
- **The session** (`COMBAT_DOCTRINE.md` §14): talk/shop → customize/test → enter event → fight the Passage → laurels, spoils, story beat. 20–40 minutes per event.
- **Sequencing note**: the shell is a *later-stage* structure (post-combat-content passes) — logged now as canon so run-layer and UI decisions stop assuming a menu-only frame; the current menu flow remains the interim shell.

**Completeness audit pass** (same session, user-directed "leave no stone unturned"): re-verified every source part against the roster and made coverage *checkable* —

- **`ARMORY_REFERENCE.md` gained §13–§16 and Appendix A**: the **codified damage model** (four range bands × ground/air stance for guns with five named range profiles; bombs flat-with-stance-and-temper; pods as pressure-not-payload; the standing exceptions), the **four schools of build** + named combo recipes (the source's build guide and combo section, themed — hangar/tutorial/AI-build doctrine, with the test-yard rule), **Stewardship** (every pattern assigned to exactly one keeper faction — melee inherits its gun-counterpart's steward, shields their bomb's; loadout freedom explicitly preserved), **Hushforged interactions** (cache-only acquisition and the Choir's Ledger, laurel halving, 115–130% budget with mandatory drawbacks, frame-directive exemption, overload applies, AI usage restricted to Choir/endgame), and the **Coverage Ledger** — every named body/gun/bomb/pod/leg in the source lists (including all illegal and Can-tier parts) mapped line-by-line to its counterpart, plus all eleven blast letters mapped to tempers. Audit result: complete; our native additions (Bombard, Dolorous Maul, Kestrel, Gryphon, Herald, Hushforged shields, scrapwright line) are marked as such.
- **`COMBAT_DOCTRINE.md` gained §11–§14**: the **controls implementation map** (built pad/keyboard layout against a source-verb coverage audit — every FAQ control concept accounted for; the one unbound verb, the collision charge, gets the directive *grounded X while locked = garniture charge*), the **layered AI framework** (Doctrine archetype × Class C/B/A/S × Personality; a uniform 12-parameter set, per-Class scaling table, composition rules with source behaviors as acceptance tests, one-blind-spot-per-archetype invariant), the **ten-pillar balance framework** (commitment-buys-damage, survivability-not-damage bodies, volley truth, counterpart parity, doctrine viability, tier economy 85/100/115–130, temper budget, 60–120 s / 2–5 knockdown pacing targets, the degenerate watchlist with its standing rules, laurels as soft balance) plus the **seeded AI-vs-AI validation harness** spec (matchup matrix, 40–60% win-rate flags — the balance CI for every parts pass), and the session-shape section.

Still docs-only; implementation continues to route through ARMORY §12's passes with the harness added alongside the AI framework work.
